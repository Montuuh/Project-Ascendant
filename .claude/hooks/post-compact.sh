#!/usr/bin/env bash
# post-compact.sh — reminder after compaction
echo "=== Post-Compact ==="
echo "Context was compacted. Re-read Notion BACKLOG before continuing."
echo "Active session state is at production/session-state/active.md"
exit 0
